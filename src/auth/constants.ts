// This is just for Demo
// In Prod, key must be protected using appropriate measures such as:
//  secrets vault, environment variable, or configuration service
export const jwtConstants = {
  secret: 'secretKey',
};
